import 'package:flutter/material.dart';

class AppColors {
  static const Color teal = Color(0xFF008080);
  static const Color navy = Color(0xFF000080);
  static const Color white = Colors.white;
  static const Color lightGrey = Color(0xFFF0F0F0);
  static const Color grey = Colors.grey;

  static const Color success = Colors.green; // positive/paid/done
  static const Color pending = Colors.orange; // pending
  static const Color error = Colors.red; // overdue/canceled/debt

  static const Color dogWalking = teal;
  static const Color boarding = navy;
  static const Color petSitting = Colors.purple;
  static const Color daycare = Colors.green;
  static const Color transport = Colors.orange;
}
